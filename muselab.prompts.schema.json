{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://muselab.dev/schemas/prompts.schema.json",
  "title": "MuseLab Locale Prompts",
  "description": "Localized scene text and choice labels stored as prompts.<locale>.json inside a .mlvn zip archive.",
  "type": "object",
  "additionalProperties": false,
  "required": ["nodes", "edges"],
  "properties": {
    "nodes": {
      "type": "object",
      "description": "Scene dialogue keyed by node id.",
      "additionalProperties": {
        "type": "object",
        "additionalProperties": false,
        "properties": {
          "textTemplate": {
            "type": "string",
            "description": "Scene dialogue/narration template (HTML subset in the designer)."
          }
        }
      }
    },
    "edges": {
      "type": "object",
      "description": "Player choice labels keyed by edge id.",
      "additionalProperties": {
        "type": "object",
        "additionalProperties": false,
        "properties": {
          "optionText": {
            "type": "string",
            "description": "Player-facing choice label. Omit or leave empty for auto-advance."
          }
        }
      }
    }
  }
}
